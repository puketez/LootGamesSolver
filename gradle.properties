modVersion=1.0.0
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# RetroFuturaGradle needs a JDK 21+ Gradle daemon. If yours is older, point at
# one from your own ~/.gradle/gradle.properties rather than here, so the path
# stays out of the repo:
#   org.gradle.java.home=/path/to/jdk21
